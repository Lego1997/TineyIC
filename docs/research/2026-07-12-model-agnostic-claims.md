=== SOURCE LIST ===
[S1] Authentication — Claude Code Docs (Anthropic official) -- https://code.claude.com/docs/en/authentication
[S2] Onboarding (CLI) — OpenClaw docs -- https://docs.openclaw.ai/start/wizard
[S3] Codex Authentication — OpenAI/ChatGPT official docs (canonical target of developers.openai.com/codex/auth) -- https://learn.chatgpt.com/docs/auth
[S4] Legal and compliance — Claude Code Docs (Anthropic official) -- https://code.claude.com/docs/en/legal-and-compliance
[S5] openclaw/openclaw — canonical GitHub repo (formerly Clawdbot → Moltbot → OpenClaw) -- https://github.com/openclaw/openclaw
[S6] OpenAI provider — OpenClaw docs -- https://docs.openclaw.ai/providers/openai
[S7] Model providers — OpenClaw docs (concepts/model-providers) -- https://github.com/openclaw/openclaw/blob/main/docs/concepts/model-providers.md
[S8] Codex Pricing & Limits — OpenAI/ChatGPT official docs -- https://learn.chatgpt.com/docs/pricing
[S9] Mixture-of-Agents Enhances Large Language Model Capabilities (Wang et al., Together AI, ICLR 2025) -- https://arxiv.org/abs/2406.04692
[S10] togethercomputer/MoA — Together Mixture-Of-Agents reference implementation (65.1% AlpacaEval, OSS models) -- https://github.com/togethercomputer/MoA
[S11] NousResearch/hermes-agent — model-agnostic agent harness (the agent that grows with you) -- https://github.com/NousResearch/hermes-agent
[S12] Mixture of Agents — Hermes Agent user-guide docs (presets, reference_models, aggregator, config.yaml) -- https://hermes-agent.nousresearch.com/docs/user-guide/features/mixture-of-agents
[S13] Rethinking Mixture-of-Agents: Is Mixing Different LLMs Beneficial? (Self-MoA, Li et al.) -- https://arxiv.org/abs/2502.00674
[S14] BerriAI/litellm — unified Python SDK + proxy for 100+ LLM APIs in OpenAI format -- https://github.com/BerriAI/litellm

=== VERIFIED_CLAIMS (3) ===
[0] (subscription-auth) The page asserts OpenAI's ToS approval as a bare, uncited statement -- "OpenAI explicitly supports subscription OAuth usage in external tools and workflows like OpenClaw" -- and enforces official-endpoints-only: native routes require the exact official Platform/ChatGPT HTTPS endpoint, and 'plaintext official HTTP endpoint' routes are Rejected with the credential not sent. No terms-of-service caveat, rate-limit-sharing note, or citation to an OpenAI policy accompanies the approval claim.
    EVIDENCE: "OpenAI explicitly supports subscription OAuth usage in external tools and workflows like OpenClaw." Route table: "Explicit exact official endpoint using HTTP | Rejected"; "plaintext official HTTP endpoint | Rejected | Credential is not sent".
    SRC: https://docs.openclaw.ai/providers/openai
[1] (provider-abstraction) The subscription/CLI pricing surface expresses effort/quality as discrete model tiers (Sol/Terra/Luna) plus a 'Fast mode' / Speed toggle that raises credit consumption — it does NOT document a numeric 'reasoning_effort' knob on the subscription surface (reasoning_effort appears only among the API-docs search suggestions); usage variance is attributed to 'Model choice, context, reasoning, tool use, retrieval, and caching.'
    EVIDENCE: 'Model choice, context, reasoning, tool use, retrieval, and caching all affect usage, so prompt length alone isn't a reliable estimate.' 'Fast mode consumes credits at a higher rate for supported models. See Speed for rates.' 'reasoning_effort' listed only under the docs-search 'Suggested' terms, not in the pricing body.
    SRC: https://learn.chatgpt.com/docs/pricing
[2] (subscription-auth) Hermes addresses models via provider-prefixed pairs where `openai-codex` is a first-class provider distinct from `openrouter`/`anthropic`, strongly implying a Codex/ChatGPT-subscription-backed route treated identically to API-key providers in routing — though the page does not explicitly confirm subscription vs API-key auth for it.
    EVIDENCE: Default preset lists provider `openai-codex` (model gpt-5.5) alongside provider `openrouter`; docs note only "The documentation does not specify whether 'openai-codex' uses a ChatGPT/Codex subscription versus API key authentication."
    SRC: https://hermes-agent.nousresearch.com/docs/user-guide/features/mixture-of-agents

=== PRIMARY_SOURCE_CLAIMS (81) ===
[0] (subscription-auth) Claude Code authenticates individual users with a Claude Pro or Max subscription by logging in with their Claude.ai account (no API key required); subscription OAuth credentials obtained via /login are the default credential for Pro, Max, Team, and Enterprise users, and /logout re-authenticates.
    EVIDENCE: "Claude Pro or Max subscription: log in with your Claude.ai account." and precedence item 6: "Subscription OAuth credentials from /login. This is the default for Claude Pro, Max, Team, and Enterprise users."
    SRC: https://code.claude.com/docs/en/authentication
[1] (subscription-auth) `claude setup-token` mints a one-year OAuth token that authenticates against the user's Claude subscription (requires Pro/Max/Team/Enterprise), is scoped to inference only (cannot establish Remote Control sessions), is not persisted to disk, and is consumed via the CLAUDE_CODE_OAUTH_TOKEN environment variable for non-interactive/CI use.
    EVIDENCE: "generate a one-year OAuth token with claude setup-token ... It does not save the token anywhere; copy it and set it as the CLAUDE_CODE_OAUTH_TOKEN environment variable ... This token authenticates with your Claude subscription and requires a Pro, Max, Team, or Enterprise plan. It is scoped to inference only and cannot establish Remote Control sessions."
    SRC: https://code.claude.com/docs/en/authentication
[2] (subscription-auth) Claude Code stores auth credentials in OS-specific locations: encrypted macOS Keychain on macOS; ~/.claude/.credentials.json (file mode 0600) on Linux; %USERPROFILE%\.claude\.credentials.json on Windows (relocatable via CLAUDE_CONFIG_DIR); these are managed by /login and /logout.
    EVIDENCE: "On macOS, credentials are stored in the encrypted macOS Keychain. On Linux, credentials are stored in ~/.claude/.credentials.json with file mode 0600. On Windows ... %USERPROFILE%\.claude\.credentials.json ... Claude Code manages .credentials.json through /login and /logout."
    SRC: https://code.claude.com/docs/en/authentication
[3] (provider-abstraction) Claude Code resolves credentials by a fixed 6-tier precedence: (1) cloud-provider vars, (2) ANTHROPIC_AUTH_TOKEN sent as Authorization: Bearer (for LLM gateways), (3) ANTHROPIC_API_KEY sent as X-Api-Key (direct Anthropic API), (4) apiKeyHelper script, (5) CLAUDE_CODE_OAUTH_TOKEN, (6) subscription OAuth from /login; a set ANTHROPIC_API_KEY overrides an active subscription once approved, and ANTHROPIC_BASE_URL redirects requests to a custom endpoint.
    EVIDENCE: Precedence list items 2-3 and 6; "If you have an active Claude subscription but also have ANTHROPIC_API_KEY set ... the API key takes precedence once approved." and "To route requests through a custom API endpoint, set the ANTHROPIC_BASE_URL environment variable instead."
    SRC: https://code.claude.com/docs/en/authentication
[4] (onboarding-ux) First-run onboarding: running `claude` opens a browser to log in; if it doesn't open the user presses `c` to copy the login URL; in WSL2/SSH/container sessions where the local callback server is unreachable the browser shows a login code that is pasted at the terminal's 'Paste code here if prompted' prompt.
    EVIDENCE: "On first launch, Claude Code opens a browser window for you to log in. If the browser doesn't open automatically, press c to copy the login URL ... If your browser shows a login code instead ... paste it into the terminal at the 'Paste code here if prompted' prompt. This happens ... common in WSL2, SSH sessions, and containers."
    SRC: https://code.claude.com/docs/en/authentication
[5] (subscription-auth) Login/token lifecycle: within five days of expiry Claude Code shows a non-blocking startup warning ('Your login expires in 3 days · run /login to renew', added in v2.1.203); apiKeyHelper-supplied keys refresh after 5 minutes or on an HTTP 401, tunable via CLAUDE_CODE_API_KEY_HELPER_TTL_MS, with a warning if the helper takes over 10 seconds.
    EVIDENCE: "Your login expires in 3 days · run /login to renew. Requires Claude Code v2.1.203 or later." and "by default, apiKeyHelper is called after 5 minutes or on HTTP 401 response. Set CLAUDE_CODE_API_KEY_HELPER_TTL_MS environment variable for custom refresh intervals."
    SRC: https://code.claude.com/docs/en/authentication
[6] (subscription-auth) OpenClaw's guided onboarding ('openclaw onboard') auto-detects existing AI access from three sources -- configured models, API-key environment variables, and 'supported local AI CLIs' -- and refuses to proceed until one candidate passes a real completion test; the local-CLI detection is the mechanism by which it reuses already-authenticated CLI sessions instead of demanding fresh keys.
    EVIDENCE: 'Detect configured models, API-key environment variables, and supported local AI CLIs.' -> 'Test the first detected candidate with a real completion.' Also: 'Guided onboarding establishes inference first. It detects available AI access, requires a real completion, and only then starts Crestodian.'
    SRC: https://docs.openclaw.ai/start/wizard
[7] (subscription-auth) OpenClaw's classic-wizard Model/Auth step exposes OAuth as a first-class provider auth flow alongside API key and provider-specific manual auth, so subscription/OAuth login is an explicitly supported alternative to entering an API key.
    EVIDENCE: Model/Auth step: 'pick a provider auth flow (API key, OAuth, or provider-specific manual auth), including Custom Provider ... Pick a default model.'
    SRC: https://docs.openclaw.ai/start/wizard
[8] (subscription-auth) OpenClaw treats 'ChatGPT/Codex setup' as a setup path distinct from 'OpenAI API-key setup', seeding different default models -- 'openai/gpt-5.6-sol' for ChatGPT/Codex vs 'openai/gpt-5.6' for the API key -- indicating a dedicated Codex/subscription login path separate from raw key entry.
    EVIDENCE: 'Fresh OpenAI API-key setup defaults to openai/gpt-5.6 (the bare direct-API id resolves to Sol); fresh ChatGPT/Codex setup defaults to openai/gpt-5.6-sol. Re-running setup preserves an existing explicit model, including openai/gpt-5.5.'
    SRC: https://docs.openclaw.ai/start/wizard
[9] (provider-abstraction) OpenClaw's provider abstraction normalizes custom providers into four wire-format families -- OpenAI-compatible (Chat Completions), OpenAI Responses-compatible, Anthropic-compatible, and Unknown auto-detect -- giving a concrete, small taxonomy for a model-agnostic layer rather than a per-vendor SDK zoo.
    EVIDENCE: 'Custom Provider (OpenAI-compatible, OpenAI Responses-compatible, Anthropic-compatible, or Unknown auto-detect).'
    SRC: https://docs.openclaw.ai/start/wizard
[10] (provider-abstraction) OpenClaw supports per-agent model and credential routing: 'openclaw agents add <name>' creates a separate agent with 'its own workspace, sessions, and auth profiles' and accepts a '--model' flag, so distinct agents can run on distinct models/providers under distinct credentials.
    EVIDENCE: 'Use openclaw agents add <name> to create a separate agent with its own workspace, sessions, and auth profiles.' Non-interactive flags listed: '--model, --agent-dir, --bind, --non-interactive.'
    SRC: https://docs.openclaw.ai/start/wizard
[11] (onboarding-ux) OpenClaw onboarding is verification-gated and idempotent: it persists only the verified model route (workspace/Gateway untouched), and re-running acts as a verify-and-repair pass that tests the current default model first and never silently replaces a configured model on failure -- it stops and asks how to continue.
    EVIDENCE: 'Persist only the verified model route and any credential/plugin state it requires. Workspace and Gateway settings remain untouched.' And: 'Re-running the command ... tests the current default model first, making the guided flow a verification and repair pass. A failing check never replaces the configured model automatically; onboarding stops and asks how to continue.'
    SRC: https://docs.openclaw.ai/start/wizard
[12] (subscription-auth) OpenAI's Codex CLI supports two first-class authentication methods: signing in with a ChatGPT subscription via a browser-based OAuth flow, OR using a usage-based OpenAI Platform API key.
    EVIDENCE: Docs describe two sign-in approaches — 'ChatGPT subscription login' (browser-based OAuth) and 'API key authentication' (usage-based access via OpenAI Platform API keys).
    SRC: https://learn.chatgpt.com/docs/auth
[13] (onboarding-ux) The ChatGPT sign-in is a browser-handoff OAuth flow shared across surfaces: the same flow serves the ChatGPT desktop app, Codex CLI, and IDE extension — it opens a browser window and the browser returns credentials to Codex after login.
    EVIDENCE: 'When you sign in with ChatGPT from the ChatGPT desktop app, Codex CLI, or IDE extension, the sign-in flow opens a browser window. After you sign in, the browser returns your credentials to Codex.'
    SRC: https://learn.chatgpt.com/docs/auth
[14] (subscription-auth) For headless / no-browser environments, Codex offers device-code authentication (beta), invoked with `codex login --device-auth` or by selecting 'Sign in with Device Code' in the interactive login UI.
    EVIDENCE: 'In these situations, prefer device code authentication (beta)' … 'select Sign in with Device Code. Run codex login --device-auth.'
    SRC: https://learn.chatgpt.com/docs/auth
[15] (subscription-auth) Codex caches login credentials in a plaintext file at `~/.codex/auth.json` by default, but the storage backend is configurable via the `cli_auth_credentials_store` option with allowed values `file | keyring | auto` (keyring = OS credential store, auto = OS store with fallback to file).
    EVIDENCE: 'Codex caches login details locally in a plaintext file at ~/.codex/auth.json' and 'Use cli_auth_credentials_store to control where the Codex CLI stores cached credentials' with values 'file | keyring | auto'.
    SRC: https://learn.chatgpt.com/docs/auth
[16] (subscription-auth) For ChatGPT sign-in sessions Codex stores access + refresh tokens and refreshes them automatically before expiry, so active sessions continue without a repeated browser login.
    EVIDENCE: 'For sign in with ChatGPT sessions, Codex refreshes tokens automatically during use before they expire, so active sessions usually continue without requiring another browser login.'
    SRC: https://learn.chatgpt.com/docs/auth
[17] (subscription-auth) Non-interactive/CI usage of the ChatGPT subscription auth is gated behind admin-granted permissions: in ChatGPT Enterprise workspaces admins can grant an access-token permission so members can create Codex access tokens for trusted non-interactive workflows; the page does NOT enumerate which consumer plans (Plus/Pro/Business/Edu) may sign in.
    EVIDENCE: 'In ChatGPT Enterprise workspaces, admins can grant the access token permission so permitted members can create Codex access tokens for trusted, non-interactive Codex local workflows.' Plan-tier eligibility is NOT PRESENT on the page.
    SRC: https://learn.chatgpt.com/docs/auth
[18] (subscription-auth) Anthropic's official policy explicitly prohibits third-party developers from offering Claude.ai login or routing requests through Free/Pro/Max subscription credentials on behalf of their users; subscription OAuth is reserved exclusively for ordinary use of Claude Code and other native Anthropic (first-party) applications.
    EVIDENCE: "OAuth authentication is intended exclusively for purchasers of Claude Free, Pro, Max, Team, and Enterprise subscription plans and is designed to support ordinary use of Claude Code and other native Anthropic applications." ... "Anthropic does not permit third-party developers to offer Claude.ai login or to route requests through Free, Pro, or Max plan credentials on behalf of their users."
    SRC: https://code.claude.com/docs/en/legal-and-compliance
[19] (subscription-auth) Anthropic reserves the right to enforce these authentication restrictions (including against subscription-credential routing) and may do so without prior notice.
    EVIDENCE: "Anthropic reserves the right to take measures to enforce these restrictions and may do so without prior notice."
    SRC: https://code.claude.com/docs/en/legal-and-compliance
[20] (subscription-auth) Anthropic directs developers building products or services that interact with Claude's capabilities — explicitly including Agent SDK users — to authenticate with API keys via Claude Console or a supported cloud provider, not via subscription login.
    EVIDENCE: "Developers building products or services that interact with Claude's capabilities, including those using the Agent SDK, should use API key authentication through Claude Console or a supported cloud provider."
    SRC: https://code.claude.com/docs/en/legal-and-compliance
[21] (subscription-auth) Advertised Pro and Max usage limits are scoped by policy to ordinary, individual usage of Claude Code and the Agent SDK, implying programmatic / multi-agent automated consumption sits outside the intended usage envelope.
    EVIDENCE: "Advertised usage limits for Pro and Max plans assume ordinary, individual usage of Claude Code and the Agent SDK."
    SRC: https://code.claude.com/docs/en/legal-and-compliance
[22] (provider-abstraction) Claude Code authenticates using either OAuth tokens or API keys, and can reach Claude via the first-party API (1P) or through Amazon Bedrock or Google Cloud's Agent Platform / Vertex (3P), with the user's existing commercial agreement governing each path.
    EVIDENCE: "Claude Code authenticates with Anthropic's servers using OAuth tokens or API keys." ... "Whether you're using the Claude API directly (1P) or accessing it through Amazon Bedrock or Google Cloud's Agent Platform (3P), your existing commercial agreement will apply to Claude Code usage."
    SRC: https://code.claude.com/docs/en/legal-and-compliance
[23] (subscription-auth) Legal terms are split by plan type: Free, Pro, and Max users are governed by Anthropic's Consumer Terms of Service, while Team, Enterprise, and Claude API users are governed by the Commercial Terms — so subscription-based use and API-key use fall under different contracts.
    EVIDENCE: "Commercial Terms - for Team, Enterprise, and Claude API users" ; "Consumer Terms of Service - for Free, Pro, and Max users"
    SRC: https://code.claude.com/docs/en/legal-and-compliance
[24] (subscription-auth) OpenClaw authenticates ChatGPT/Codex SUBSCRIPTIONS (not API keys) via OAuth 2.0 + PKCE: `openclaw models auth login --provider openai` opens auth.openai.com/oauth/authorize (scope `openid profile email offline_access`), captures the redirect on a loopback callback at http://localhost:1455/auth/callback (override via OPENCLAW_OAUTH_CALLBACK_HOST, with a manual code/redirect-URL paste fallback for headless/remote hosts), exchanges at auth.openai.com/oauth/token, extracts accountId from the access token, and stores `{access, refresh, expires, accountId}`. Subscription traffic then runs through ChatGPT's backend (chatgpt.com/backend-api) via a bundled native 'Codex app-server' runtime, not the OpenAI Platform API. This external-tool subscription OAuth use is officially sanctioned by OpenAI.
    EVIDENCE: oauth.md: 'open https://auth.openai.com/oauth/authorize ... scope openid profile email offline_access'; 'capture the callback on http://localhost:1455/auth/callback'; 'store { access, refresh, expires, accountId }'. openai.md: 'OpenAI explicitly supports subscription OAuth usage in external tools and workflows like OpenClaw'; attribution headers attach 'on native Codex traffic to chatgpt.com/backend-api'. Corroborated by press: OpenAI opened ChatGPT subscriptions to OpenClaw.
    SRC: https://github.com/openclaw/openclaw
[25] (subscription-auth) For Anthropic, OpenClaw does NOT run its own Claude OAuth; it REUSES the locally installed Claude Code CLI's credentials (`claude -p`) and/or an Anthropic 'setup-token', keeps the model ref canonical (`anthropic/...`), and prefers CLI reuse when a local Claude login exists. The ToS posture is contested/fragile and asymmetric to OpenAI's: OpenClaw's repo asserts 'Anthropic staff told us this usage is allowed again' and treats Claude-CLI reuse as sanctioned 'unless Anthropic publishes a new policy,' while recommending API-key auth as 'the safer recommended path' for production — yet independent April-2026 reporting says Anthropic BLOCKED Claude subscription access to OpenClaw (the opposite of OpenAI's official opening).
    EVIDENCE: anthropic-provider-path.md lists 'Claude CLI credential reuse, Setup-token auth'. oauth.md: 'OpenClaw treats Claude CLI reuse and claude -p usage as sanctioned for this integration unless Anthropic publishes a new policy'; 'API key auth is still the safer recommended path'. Secondary: TNW/CNBC report 'Anthropic blocks Claude access' / 'block Claude subscriptions from OpenClaw in April' 2026.
    SRC: https://github.com/openclaw/openclaw
[26] (subscription-auth) Subscription OAuth tokens are stored per-agent in the agent's SQLite DB (surfaced as `~/.openclaw/agents/<agentId>/agent/auth-profiles.json`), refreshed under a file lock, and are deliberately NON-portable across agents because refresh tokens rotate/invalidate. OpenClaw documents a concrete hazard for subscription reuse: because providers 'mint a new refresh token on every login/refresh' and invalidate the prior one, logging in via BOTH OpenClaw and the official Claude Code/Codex CLI can mutually log each other out; OpenClaw mitigates by treating its local profile as the canonical 'token sink' once it owns OAuth (and re-reads externally-managed CLI creds instead of spending a copied refresh token). Static SecretRef indirection is rejected for OAuth material because it is runtime-mutable.
    EVIDENCE: oauth.md: 'OAuth providers commonly mint a new refresh token on every login/refresh ... the local refresh token is canonical'; storage path given. auth-credential-semantics.md: 'oauth profiles are not portable by default because refresh tokens can be single-use or rotation-sensitive'; 'OAuth credentials are runtime-mutable ... SecretRef objects are rejected'.
    SRC: https://github.com/openclaw/openclaw
[27] (provider-abstraction) OpenClaw is model-agnostic via its OWN provider-plugin registry (`registerProvider(...)`) over a generic inference loop — NOT by adopting LiteLLM/aisuite as the core abstraction. ~100+ providers (OpenAI, Anthropic, Google, OpenRouter, DeepSeek, Mistral, Cohere, Groq, Cerebras, Together, Fireworks, xAI, MiniMax, Z.AI/GLM, Qwen, Moonshot/Kimi, Bedrock, Azure/Foundry, Ollama/vLLM/SGLang/LM Studio, etc.) are each a plugin owning its catalog, auth env-var mapping, OAuth refresh, transport, failover, and thinking profile. Models are addressed as `provider/model` (e.g. `anthropic/claude-opus-4-6`). LiteLLM, OpenRouter, Vercel AI Gateway and Cloudflare AI Gateway appear only as individual bundled provider plugins, not the abstraction layer. Multi-key rotation is env-driven (`<PROVIDER>_API_KEY[S]`, numbered `<PROVIDER>_API_KEY_1`, `OPENCLAW_LIVE_<PROVIDER>_KEY`), retried only on rate-limit responses.
    EVIDENCE: model-providers.md: 'Most provider-specific logic lives in provider plugins (registerProvider(...)) while OpenClaw keeps the generic inference loop.' Model ref pattern 'provider/model'. docs/providers/ includes litellm.md, openrouter.md, vercel-ai-gateway.md, cloudflare-ai-gateway.md as peers among ~90 provider pages; env-key rotation + rate-limit-only retry documented.
    SRC: https://github.com/openclaw/openclaw
[28] (provider-abstraction) OpenClaw already implements the exact 'normalized thinking-effort' control our brief specifies: a single canonical ladder `off|minimal|low|medium|high|xhigh|adaptive|max|ultra` (set via `/think <level>`, CLI `--thinking high`, or per-agent/global config defaults) that each provider plugin maps to native controls — OpenAI Responses `reasoning.effort` (emitting `none` for off only when supported), Anthropic effort + adaptive extended thinking (`output_config.effort`; `/think xhigh|max` on Opus 4.7+), Google Gemini `thinkingBudget:-1` / `thinkingLevel`, DeepSeek `reasoning_effort`, Ollama native `think` (low/medium/high), Z.AI/GLM `reasoning_effort`. Plugins declare supported levels via `resolveThinkingProfile(ctx)`; typed levels a model doesn't support are rejected with valid options or remapped to the nearest supported non-off level.
    EVIDENCE: thinking.md: level list 'off | minimal | low | medium | high | xhigh | adaptive | max | ultra'; 'GPT models map /think through model-specific Responses API effort support'; 'Gemini 2.5 requests send thinkingBudget: -1'; 'Anthropic Claude Opus 4.7+ maps /think xhigh to adaptive thinking plus output_config.effort'; 'Provider plugins can expose resolveThinkingProfile(ctx)'.
    SRC: https://github.com/openclaw/openclaw
[29] (moa) OpenClaw does NOT implement a Together-AI-style mixture-of-agents (no proposer-layer + aggregator synthesis is described in the docs), but it DOES provide the substrate a per-persona-different-model committee needs: each 'agent' is an isolated persona with its OWN model registry AND its own auth profiles/accounts under `~/.openclaw/agents/<agentId>/...`, so different personas can run on different providers/models — and even different subscription accounts — concurrently. 'Parallel specialist lanes' plus a command queue serialize runs per session and cap global concurrency (`agents.defaults.maxConcurrent`, `subagents.maxConcurrent`, `delegationMode: prefer`), and an optional lightweight 'coordinator/traffic controller' lane routes handoffs. This is a router/orchestrator model, not layer-wise MoA aggregation, so a synthesizer/memo-writer step would have to be added by us.
    EVIDENCE: multi-agent.md: 'An agent is the full per-persona scope: workspace files, auth profiles, model registry, and session store'; per-agent auth at agents/<agentId>/agent/auth-profiles.json. parallel-specialist-lanes.md: maxConcurrent/subagents config + Phase 3 'coordinator / traffic controller'. No proposer/aggregator MoA construct present.
    SRC: https://github.com/openclaw/openclaw
[30] (subscription-auth) OpenClaw uses a single provider id (`openai`) and canonical `openai/*` model route for BOTH direct API-key auth and ChatGPT/Codex subscription auth; subscription sign-in is a first-class command: `openclaw models auth login --provider openai` (localhost browser OAuth callback), with `--device-code` for headless/callback-hostile setups, and `--profile-id openai:<name>` to hold multiple Codex OAuth logins selectable via `/model ...@<profileId>`.
    EVIDENCE: "OpenClaw uses one provider id, `openai`, for both direct API-key auth and ChatGPT/Codex subscription auth." Headless: "add `--device-code` to sign in with a ChatGPT device-code flow instead of the localhost browser callback."
    SRC: https://docs.openclaw.ai/providers/openai
[31] (subscription-auth) Subscription OAuth credentials are transported to the model runtime via an internal 'app-server login RPC', never as env-var API keys over the WebSocket connection; OpenClaw can also BRIDGE an existing local Codex CLI ChatGPT sign-in into its app-server through that login RPC without sharing the CLI's config, plugins, or thread store. Auth-selection order is: (1) ordered `auth.order.openai` profiles, (2) the app-server's existing account / bridged local Codex CLI login, (3) local-stdio-no-account only: `CODEX_API_KEY` then `OPENAI_API_KEY`.
    EVIDENCE: "OpenClaw bridges that native CLI account into the app-server through its login RPC; it does not share the CLI's config, plugins, or thread store." "[the env API-key fallback] is never sent over WebSocket app-server connections... sends the selected credentials through the app-server login RPC instead."
    SRC: https://docs.openclaw.ai/providers/openai
[32] (provider-abstraction) OpenClaw exposes a normalized reasoning-effort control (`reasoning.effort`) but GATES allowed values per-model by capability rather than assuming a universal scale: GPT-5.6 sol/terra/luna expose `xhigh` and `max`; native routes keep `reasoning: { effort: "none" }` only for models that support OpenAI's `none` effort and OMIT the disabled-reasoning field entirely for models/proxies that reject `reasoning.effort: "none"`. A separate `reasoningEffort` config path exists for realtime voice.
    EVIDENCE: "All three expose `xhigh` and `max` reasoning in the current catalog." Native routes: "Keep `reasoning: { effort: "none" }` only for models that support the OpenAI `none` effort" / "Omit disabled reasoning for models or proxies that reject `reasoning.effort: "none"`".
    SRC: https://docs.openclaw.ai/providers/openai
[33] (provider-abstraction) OpenClaw treats provider, model, runtime, and channel as four SEPARATE config layers: `agentRuntime.id` (`codex` vs `openclaw`) selects execution runtime independently of credential type/billing, and `auth.order.openai: [profileA, profileB]` orders multiple profiles so the subscription is tried first with an API-key backup while staying on the same harness. When a subscription profile hits a Codex usage limit, OpenClaw marks it blocked until Codex's advertised reset time and rotates to the next `openai:*` profile WITHOUT changing the selected model or dropping the harness.
    EVIDENCE: "Provider, model, runtime, and channel are separate layers." "OpenClaw tries the subscription first, then the API key, while staying on the Codex harness." "marks the profile blocked until Codex's advertised reset time and lets auth ordering rotate to the next `openai:*` profile, without changing the selected model or dropping out of the Codex harness."
    SRC: https://docs.openclaw.ai/providers/openai
[34] (onboarding-ux) Onboarding is a two-branch chooser: `openclaw onboard --auth-choice openai-api-key` ("Best for: direct API access and usage-based billing") vs `openclaw onboard --auth-choice openai` ("Best for: using your ChatGPT/Codex subscription... instead of a separate API key"). Each path ends in a verify step (`openclaw models list --provider openai`); the Codex path adds in-chat verification (`/codex status`, `/codex models`) and a `/status` runtime indicator that reads "Runtime: OpenAI Codex". A "Quick choice" table maps Goal -> model ref -> notes.
    EVIDENCE: Two Getting-started sections: "API key (OpenAI Platform)" and "Codex subscription", each with a "Best for:" line. "Chat `/status` shows which model runtime is active... appears as `Runtime: OpenAI Codex`."
    SRC: https://docs.openclaw.ai/providers/openai
[35] (subscription-auth) OpenClaw treats a ChatGPT/Codex subscription as a first-class auth path distinct from API keys: users authenticate via OAuth ('openclaw onboard --auth-choice openai' or 'openclaw models auth login --provider openai'), which routes through a 'native Codex app-server harness' to chatgpt.com/backend-api, and some models (e.g. 'openai/gpt-5.6-sol', 'openai/gpt-5.3-codex-spark') are available ONLY through ChatGPT/Codex OAuth and are rejected on direct OpenAI/Azure API-key routes.
    EVIDENCE: 'openai/gpt-5.3-codex-spark is available only through ChatGPT/Codex OAuth; direct OpenAI API-key and Azure API-key routes reject it'; 'Auth: OAuth (ChatGPT)'; 'Fresh native Codex app-server harness ref: openai/gpt-5.6-sol'; attribution headers attach only 'on native Codex traffic to chatgpt.com/backend-api'.
    SRC: https://github.com/openclaw/openclaw/blob/main/docs/concepts/model-providers.md
[36] (subscription-auth) For an Anthropic subscription, OpenClaw does NOT implement Claude OAuth itself — it reuses the user's already-authenticated Claude CLI by shelling out ('claude -p'), configured as a canonical model ref plus a per-model runtime override ('anthropic/claude-opus-4-8' with 'agentRuntime.id: "claude-cli"'); this is explicitly called a 'sanctioned OpenClaw integration path', with Anthropic setup-token auth also supported as an alternative.
    EVIDENCE: 'Claude CLI reuse (`claude -p`) is a sanctioned OpenClaw integration path. Anthropic setup-token auth remains supported, but OpenClaw prefers Claude CLI reuse when available.'; config uses 'anthropic/claude-opus-4-8 with model-scoped agentRuntime.id: "claude-cli"'.
    SRC: https://github.com/openclaw/openclaw/blob/main/docs/concepts/model-providers.md
[37] (subscription-auth) Subscription/OAuth tokens are stored by the underlying CLI, not in OpenClaw's own config: the Gemini CLI login flow 'stores tokens in auth profiles on the gateway host' and users 'do not paste a client id or secret into openclaw.json'. The doc's ToS posture is asymmetric — it warns ONLY about Gemini CLI OAuth as an 'unofficial integration' with account-restriction risk, while presenting ChatGPT/Codex OAuth (native app-server) and Claude CLI reuse without any such warning.
    EVIDENCE: 'You do not paste a client id or secret into openclaw.json. The CLI login flow stores tokens in auth profiles on the gateway host.'; Warning: 'Gemini CLI OAuth in OpenClaw is an unofficial integration. Some users have reported Google account restrictions after using third-party clients... use a non-critical account'. No equivalent warning appears on the Codex OAuth or Claude CLI sections.
    SRC: https://github.com/openclaw/openclaw/blob/main/docs/concepts/model-providers.md
[38] (provider-abstraction) OpenClaw does not depend on an external multi-provider abstraction (LiteLLM/aisuite/any-llm); it keeps a 'generic inference loop' and pushes provider-specific logic into its own plugin SDK via 'registerProvider(...)', where each plugin owns onboarding, model catalogs, auth env-var mapping, OAuth refresh, failover classification, and thinking/reasoning profiles. LiteLLM/OpenRouter/Vercel AI Gateway appear only as optional pass-through providers you point a custom base URL at, not as the internal abstraction.
    EVIDENCE: 'Most provider-specific logic lives in provider plugins (registerProvider(...)) while OpenClaw keeps the generic inference loop. Plugins own onboarding, model catalogs, auth env-var mapping, ... OAuth refresh, usage reporting, thinking/reasoning profiles'; LiteLLM listed under 'Local proxies (LM Studio, vLLM, LiteLLM, etc.)' as a custom baseUrl target.
    SRC: https://github.com/openclaw/openclaw/blob/main/docs/concepts/model-providers.md
[39] (provider-abstraction) OpenClaw normalizes 'thinking effort' as a session-level control ('/think adaptive', a 'session thinking level') that each provider plugin translates into that provider's native parameter: Gemini 3/3.1 omit a fixed 'thinkingLevel' while Gemini 2.5 sends 'thinkingBudget: -1'; MiniMax M2.x has thinking disabled by default while M3.x stays on omitted/adaptive; vLLM nemotron sends 'enable_thinking: false' when the session thinking level is off — i.e. one shared knob maps to per-provider thinking params.
    EVIDENCE: 'Thinking: /think adaptive uses Google dynamic thinking. Gemini 3/3.1 omit a fixed thinkingLevel; Gemini 2.5 sends thinkingBudget: -1.'; 'OpenClaw disables thinking by default for the M2.x family... MiniMax-M3 (and M3.x) stays on the provider's omitted/adaptive thinking path'; 'the bundled vLLM plugin automatically sends enable_thinking: false ... when the session thinking level is off'.
    SRC: https://github.com/openclaw/openclaw/blob/main/docs/concepts/model-providers.md
[40] (onboarding-ux) Onboarding is driven by a single 'openclaw onboard --auth-choice <choice>' primitive that forks per provider into either an API-key path or a subscription/OAuth path — e.g. 'openai-api-key' vs 'openai' (ChatGPT OAuth), 'apiKey' (Anthropic key) vs Claude CLI reuse, 'minimax-global-api' vs 'minimax-global-oauth'; provider-scoped model pickers fall back to the unfiltered catalog when a provider's models aren't loaded yet rather than showing an empty picker.
    EVIDENCE: CLI examples throughout: 'openclaw onboard --auth-choice openai-api-key', '--auth-choice openai', '--auth-choice apiKey', 'minimax-global-oauth' / 'minimax-global-api'; 'OpenClaw falls back to the unfiltered catalog instead of showing an empty provider-scoped picker.'
    SRC: https://github.com/openclaw/openclaw/blob/main/docs/concepts/model-providers.md
[41] (subscription-auth) OpenAI's official Codex CLI supports two distinct, first-class auth/billing paths: (a) signing in with a ChatGPT subscription (Free/Go/Plus/Pro/Business/Enterprise/Edu) where Codex usage is INCLUDED in the plan, or (b) an 'API Key' path billed per-token at standard API pricing; the CLI, SDK, and IDE extension all work under either path, but cloud features (GitHub review, Slack) require the subscription path.
    EVIDENCE: Page description: 'ChatGPT Work and Codex are included in your ChatGPT Free, Go, Plus, Pro, Business, Edu, or Enterprise plan.' API Key card: 'Codex in the CLI, SDK, or IDE extension / No cloud-based features (GitHub code review, Slack, etc.) / Model availability follows the API models available to your key / Pay only for the tokens Codex uses, based on API pricing' (with a 'Learn more' link to /docs/auth).
    SRC: https://learn.chatgpt.com/docs/pricing
[42] (subscription-auth) Subscription-based Codex usage is metered as 'Local Messages per 5-hour window' (a rolling window, plus possible weekly limits), NOT per-token: e.g. ChatGPT Plus allows ~20-110 GPT-5.6 Terra local messages / 5h; Pro offers explicit '5x' or '20x' higher limits than Plus; only the API-Key path is billed 'Usage-based' per token.
    EVIDENCE: Plus table: 'GPT-5.6 Terra | 20-110' Local Messages/5h; Pro tiers labeled 'Pro 5x' and 'Pro 20x' ('5x or 20x more Codex usage than Plus'); footnote: 'The usage limits for local messages and cloud tasks share a five-hour window. Additional weekly limits may apply.' API Key table lists every model as 'Usage-based'.
    SRC: https://learn.chatgpt.com/docs/pricing
[43] (onboarding-ux) Codex CLI surfaces remaining subscription usage in-session via a '/status' command and a web usage dashboard, and OpenAI officially recommends switching to a smaller/cheaper model to stretch limits, plus running overflow tasks on an API key at standard API rates — so subscription and API-key billing can coexist as a fallback.
    EVIDENCE: 'If you want to see your remaining limits during an active Codex CLI session, you can use /status.' 'If you are approaching usage limits, you can also switch to a smaller model to make your usage limits last longer.' 'All users may also run extra local tasks using an API key, with usage charged at standard API rates.' Plus/Pro users 'can purchase additional credits to continue working without needing to upgrade'.
    SRC: https://learn.chatgpt.com/docs/pricing
[44] (provider-abstraction) Even within one provider, OpenAI ships a model-tier abstraction users pick per task: the GPT-5.6 family splits into Sol (max quality/reasoning depth), Terra (balanced everyday default), and Luna (speed/affordability), alongside legacy GPT-5.5 / GPT-5.4 / GPT-5.4-mini and a Pro-only GPT-5.3-Codex-Spark research preview, each with its own rate limit and credit cost.
    EVIDENCE: 'Sol is the best choice when quality and reasoning depth matter most... Terra is the everyday default: strong capability with a better balance of performance and price... Luna is optimized for speed and affordability, making it a good fit for lighter-weight or high-volume workloads.' Rate card: Sol 125/750 credits per 1M in/out tokens vs Luna 25/150; 'GPT-5.3-Codex-Spark is in research preview for ChatGPT Pro users only, and isn't available in the API at launch.'
    SRC: https://learn.chatgpt.com/docs/pricing
[45] (subscription-auth) Image generation and other agentic features draw from the SAME shared Codex usage limits/credits (e.g. ChatGPT for Excel shares the pool; images consume limits '3-5x faster'), so any multi-agent workload competes for one subscription budget rather than isolated per-feature quotas.
    EVIDENCE: 'Codex usage limits are shared with other agentic features once pricing for those features is effective. This currently includes ChatGPT for Excel on Plus and Pro.' 'Image generations use included limits 3-5x faster on average.' Banner: 'ChatGPT Work and Codex share usage. Work usage inside ChatGPT uses the same pricing, credits, and usage limits as Codex.'
    SRC: https://learn.chatgpt.com/docs/pricing
[46] (moa) MoA is a layered architecture: the default config uses 3 layers of 6 heterogeneous LLM agents each, where every agent in a layer receives ALL outputs from the previous layer as auxiliary context before generating its own response, and a final aggregator synthesizes one answer. Two roles exist: 'proposers' (generate diverse reference responses) and 'aggregators' (synthesize proposer outputs into a single high-quality response).
    EVIDENCE: "We construct 3 MoA layers and use the same set of models in each MoA layer"; "Each agent takes all the outputs from agents in the previous layer as auxiliary information in generating its response." Proposers "excel at generating useful reference responses"; aggregators are "models proficient in synthesizing responses from other models into a single, high-quality output."
    SRC: https://arxiv.org/abs/2406.04692
[47] (moa) MoA explicitly mixes DIFFERENT open-source models within one committee. The main configuration draws its 6 agents from: Qwen1.5-110B-Chat, Qwen1.5-72B-Chat, WizardLM-8x22B, LLaMA-3-70B-Instruct, Mixtral-8x22B-v0.1, and dbrx-instruct — demonstrating a heterogeneous multi-provider/multi-model ensemble rather than N copies of one model.
    EVIDENCE: Six models employed: Qwen1.5-110B-Chat, Qwen1.5-72B-Chat, WizardLM-8x22B, LLaMA-3-70B-Instruct, Mixtral-8x22B-v0.1, dbrx-instruct. "Each agent can be the same model or different ones."
    SRC: https://arxiv.org/abs/2406.04692
[48] (moa) Proposer skill and aggregator skill are separable and model-specific: some models are strong proposers but poor aggregators, while others are versatile at both. WizardLM was an excellent proposer but weak aggregator; GPT-4o, Qwen1.5, and LLaMA-3 were effective at both proposing and aggregating.
    EVIDENCE: "WizardLM demonstrated excellent performance as an proposer model but struggled to maintain its effectiveness in aggregating responses." "GPT-4o, Qwen1.5, LLaMA-3 emerged as a versatile model effective in both assisting and aggregating tasks."
    SRC: https://arxiv.org/abs/2406.04692
[49] (moa) The core empirical finding ('collaborativeness of LLMs') is that a model produces better responses when shown outputs from other models — even when those other models are individually less capable. This is the mechanism that justifies a multi-agent debate over a single model.
    EVIDENCE: "an LLM tends to generate better responses when presented with outputs from other models, even if these other models are less capable by itself."
    SRC: https://arxiv.org/abs/2406.04692
[50] (moa) An MoA ensemble of only open-source models beats a single frontier model: MoA scored 65.1% length-controlled win rate on AlpacaEval 2.0 versus 57.5% for GPT-4 Omni (a ~7.6-point gap), and also led MT-Bench (9.25 avg) and FLASK.
    EVIDENCE: "our MoA using only open-source LLMs is the leader of AlpacaEval 2.0 by a substantial gap, achieving a score of 65.1% compared to 57.5% by GPT-4 Omni." MT-Bench 9.25 avg; FLASK leading across most skill dimensions.
    SRC: https://arxiv.org/abs/2406.04692
[51] (moa) A cheaper 'MoA-Lite' variant works: 2 layers with Qwen1.5-72B-Chat as the sole aggregator still improved AlpacaEval 2.0 quality (~1.8%) over GPT-4o while being more cost-effective — meaning even a shallow (2-round) committee with one designated synthesizer yields gains. The aggregator prompt instructs it to critically evaluate inputs, warning that some may be biased or incorrect.
    EVIDENCE: "2 MoA layers and employs Qwen1.5-72B-Chat as the aggregator" achieving "1.8% improvement in quality on AlpacaEval 2.0" while being more economical. Aggregator prompt: "synthesize these responses into a single, high-quality response. It is crucial to critically evaluate the information provided in these responses, recognizing that some of it may be biased or incorrect."
    SRC: https://arxiv.org/abs/2406.04692
[52] (moa) MoA's core architecture is a layered committee of 'proposer' models running in parallel feeding an 'aggregator' that synthesizes them; the quickstart default is 2 layers (4 proposers in layer 1 -> 1 aggregator in layer 2), and depth is extended to 3+ layers via a --rounds refinement parameter.
    EVIDENCE: README: 'MoA ... 2 layers and 4 LLMs'; moa.py: `results = await asyncio.gather(*[run_llm(model) for model in reference_models])` then a single `client.chat.completions.create(model=aggregator_model, ...)`; CLI exposes `--aggregator`, `--reference_models`, `--rounds` (refinement layer count).
    SRC: https://github.com/togethercomputer/MoA
[53] (moa) Each agent slot runs a DIFFERENT model: the quickstart reference_models list is ['meta-llama/Llama-3.3-70B-Instruct-Turbo', 'Qwen/Qwen2.5-72B-Instruct-Turbo', 'Qwen/Qwen2.5-Coder-32B-Instruct', 'microsoft/WizardLM-2-8x22B'] with aggregator_model = 'Qwen/Qwen2.5-72B-Instruct-Turbo' — proving heterogeneous per-agent model assignment is the intended design, and the aggregator may reuse one of the proposer models.
    EVIDENCE: moa.py reference_models list (four distinct model identifier strings) and separate `aggregator_model = "Qwen/Qwen2.5-72B-Instruct-Turbo"`.
    SRC: https://github.com/togethercomputer/MoA
[54] (moa) Aggregation is done purely via prompt injection, not weights: inject_references_to_messages() deep-copies the message list and prepends a system prompt equal to the aggregator prompt plus a numbered concatenation of every proposer's full response ('1. <resp>\n2. <resp> ...'); the synthesis prompt tells the aggregator to 'synthesize these responses into a single, high-quality response' and to 'critically evaluate the information ... recognizing that some of it may be biased or incorrect' rather than replicate.
    EVIDENCE: utils.py aggregator system prompt 'You have been provided with a set of responses from various open-source models to the latest user query. Your task is to synthesize these responses ...'; inject_references_to_messages formats references as numbered list items concatenated into the system prompt.
    SRC: https://github.com/togethercomputer/MoA
[55] (provider-abstraction) The reference implementation is plain OpenAI-SDK code pointed at an OpenAI-compatible base_url — `openai.OpenAI(api_key=os.environ.get('TOGETHER_API_KEY'), base_url='https://api.together.xyz/v1')` — with distinct generate_together / generate_together_stream / generate_openai paths keyed on the model string; it is provider-swappable by base_url but ships NO unified multi-provider router, defaulting all proposers to a single Together endpoint.
    EVIDENCE: utils.py client construction with Together base_url + TOGETHER_API_KEY; separate generate_together(), generate_together_stream() (OpenAI client to Together), and generate_openai() (OpenAI client to OpenAI) functions; moa.py routes all reference_models through one client.
    SRC: https://github.com/togethercomputer/MoA
[56] (moa) MoA using only open-source models beats a single frontier model on quality benchmarks: 65.1% on AlpacaEval 2.0 (length-controlled win rate) vs GPT-4 Omni's 57.5%, plus top MT-Bench positions and beating Qwen1.5-110B-Chat across FLASK axes (harmlessness, robustness, correctness, efficiency, factuality).
    EVIDENCE: README: 'MoA significantly outperforms GPT-4 Omni's 57.5% on AlpacaEval 2.0 with a score of 65.1%'; FLASK/MT-Bench claims.
    SRC: https://github.com/togethercomputer/MoA
[57] (moa) Operational defaults in the reference code are max_tokens=2048 and temperature=0.7 (coerced to 0 when temperature < 1e-4), with exponential-backoff retry (1,2,4,8,16,32s) and invalid_request_error returning None; the repo is Apache-2.0 licensed and exposes NO cross-provider reasoning/thinking-effort abstraction.
    EVIDENCE: utils.py: max_tokens 2048, temperature 0.7 default, exponential backoff sequence; README license 'Apache 2.0'; only --temperature/--max_tokens knobs exist, no reasoning_effort/thinking-budget parameter.
    SRC: https://github.com/togethercomputer/MoA
[58] (subscription-auth) Hermes Agent lets users attach their ChatGPT/Codex subscription via 'ChatGPT OAuth' (run `hermes model` -> OpenAI Codex, or `hermes auth add codex-oauth` for a device-code login) instead of an API key; it stores tokens in its own `~/.hermes/auth.json` and will import existing Codex CLI credentials from `~/.codex/auth.json` when present, with no Codex CLI install required, and rotates refresh tokens transparently (revoked tokens are quarantined).
    EVIDENCE: Providers table: 'OpenAI Codex | `hermes model` (ChatGPT OAuth...)'. 'stores credentials in its own auth store under ~/.hermes/auth.json and can import existing Codex CLI credentials from ~/.codex/auth.json when present. No Codex CLI installation...'. 'Run `hermes auth add codex-oauth` ... to start a fresh device-code login'.
    SRC: https://github.com/NousResearch/hermes-agent
[59] (subscription-auth) Hermes authenticates Anthropic via OAuth marketed as 'Claude Max + extra usage credits', routing 'as Claude Code' and preferring Claude Code's own credential store (auto-detecting Claude Code credential files / `hermes auth add anthropic --type oauth`); it explicitly distinguishes this subscription path from an Anthropic API key, which bills at 'standard API pricing, independent of any Claude subscription'.
    EVIDENCE: 'Anthropic | `hermes model` (Claude Max + extra usage credits via OAuth; also supports Anthropic API key or manual setup-token)'. 'When you authenticate via `hermes model` -> Anthropic OAuth ... Hermes routes as Claude Code'. 'Hermes prefers Claude Code's own credential store'. API key path bills 'against that key's organization (standard API pricing, independent of any Claude subscription)'.
    SRC: https://github.com/NousResearch/hermes-agent
[60] (onboarding-ux) A single `hermes model` terminal wizard is the full onboarding surface (add providers, run OAuth flows, enter API keys, configure custom endpoints), while in-session `/model` only switches among already-configured providers and cannot run OAuth; the OAuth roster spans consumer subscriptions beyond OpenAI/Anthropic -- GitHub Copilot (device-code login, 'the same flow used by the Copilot CLI and opencode'), xAI SuperGrok/Premium+, Qwen Portal and MiniMax (browser PKCE), Google Vertex, Ollama Cloud -- plus a one-command `hermes setup --portal` OAuth onboarding for Nous Portal.
    EVIDENCE: '`hermes model` | Your terminal (outside any session) | Full setup wizard -- add providers, run OAuth, enter API keys, configure endpoints'; `/model` 'cannot add new providers, run OAuth, or prompt for API keys'. Copilot: 'OAuth device code login -- the same flow used by the Copilot CLI and opencode'. Qwen/MiniMax: 'browser PKCE login'. '`hermes setup --portal` logs you in via OAuth'.
    SRC: https://github.com/NousResearch/hermes-agent
[61] (provider-abstraction) Hermes implements its own multi-transport provider layer rather than delegating to a generic router: `custom_providers[]` entries carry `base_url`, `key_env`, and an `api_mode` of `chat_completions` or `anthropic_messages` (plus a `codex_responses` Responses-API transport), all driven from one `~/.hermes/config.yaml`; a top-level `fallback_providers` chain swaps to backup provider+model on 4xx/5xx or auth errors mid-session without losing history. (LiteLLM Proxy/ClawRouter are supported only as optional downstream endpoints, not the core abstraction.)
    EVIDENCE: `custom_providers: - name: corp ... api_mode: chat_completions`; `api_mode: anthropic_messages  # for Anthropic-compatible proxies`. 'xAI is wired through the Responses API (`codex_responses` transport)'. 'canonical format is a top-level `fallback_providers:` list'. Fallback 'swaps models mid-session without losing conversation history.'
    SRC: https://github.com/NousResearch/hermes-agent
[62] (moa) Hermes treats 'MoA' as a first-class auxiliary task (grouped with vision and web summarization) that is routed to an independently configurable model via `auxiliary.*.provider`; the default `"auto"` reuses the main chat model, but each task can be overridden to a different provider/model (e.g. Gemini Flash on OpenRouter) -- i.e. a distinct subtask can run on a different model than the main agent.
    EVIDENCE: 'some tools (vision, web summarization, MoA) use a separate "auxiliary" model. By default (`auxiliary.*.provider: "auto"`), Hermes routes these tasks to your main chat model ... You can override each task individually to' a 'cheaper/faster model (e.g. Gemini Flash on OpenRouter)'.
    SRC: https://github.com/NousResearch/hermes-agent
[63] (provider-abstraction) Hermes exposes one normalized reasoning control, `agent.reasoning_effort`, with a provider-agnostic ladder `xhigh (max) | high | medium | low | minimal | none (disable)`, and adapts it per provider under the hood: xAI Grok 4 (Responses API) 'reasons by default -- no reasoning_effort parameter needed', while self-hosted vLLM/Gemma take `extra_body.reasoning_effort` / `enable_thinking` (or `chat_template_kwargs.enable_thinking`) and Qwen servers surface `reasoning_content` trace metadata.
    EVIDENCE: config: `agent: reasoning_effort: "medium"  # Options: "xhigh" (max), "high", "medium", "low", "minimal", "none" (disable)`. Grok: 'no `reasoning_effort` parameter needed, the server reasons by default.' vLLM: `extra_body: enable_thinking: true / reasoning_effort: high`. Qwen parsers preserve `reasoning_content`.
    SRC: https://github.com/NousResearch/hermes-agent
[64] (moa) Hermes Agent's MoA is a single-layer proposer/aggregator design: multiple 'reference models' (advisors) run in parallel WITHOUT tool schemas on the conversation text, and their outputs are appended as 'private context' to the latest user turn for a single designated 'aggregator' model, which alone makes the real response and tool calls. Recursive/multi-layer MoA presets are disallowed.
    EVIDENCE: Docs: "runs the configured reference models without tool schemas... appends the reference outputs as private context for the aggregator; calls the configured aggregator with the normal Hermes tool schema." Architecture note: "Single layer (no recursive MoA presets allowed)."
    SRC: https://hermes-agent.nousresearch.com/docs/user-guide/features/mixture-of-agents
[65] (provider-abstraction) MoA config stores explicit provider/model pairs so each reference model and the aggregator can be a DIFFERENT provider; the shipped default preset uses reference models openai-codex:gpt-5.5 and openrouter:deepseek/deepseek-v4-pro with aggregator openrouter:anthropic/claude-opus-4.8.
    EVIDENCE: "The config stores explicit provider/model pairs, so you can mix providers and use multiple models from the same provider." Default preset YAML: reference_models = [{provider: openai-codex, model: gpt-5.5}, {provider: openrouter, model: deepseek/deepseek-v4-pro}]; aggregator = {provider: openrouter, model: anthropic/claude-opus-4.8}.
    SRC: https://hermes-agent.nousresearch.com/docs/user-guide/features/mixture-of-agents
[66] (moa) MoA is configured under a `moa` root key with `default_preset` plus named `presets`; each preset has `reference_models` (list of provider/model dicts), an `aggregator` (provider/model dict), and optional tuning keys `reference_max_tokens`, `aggregator_temperature`, `reference_temperature`, `max_tokens`, and `enabled`.
    EVIDENCE: config.yaml structure shows `moa: default_preset: default / presets: [name]: reference_models / aggregator / reference_max_tokens / aggregator_temperature / reference_temperature / max_tokens: 4096 / enabled`.
    SRC: https://hermes-agent.nousresearch.com/docs/user-guide/features/mixture-of-agents
[67] (moa) MoA latency is dominated by advisor output volume (reference models run in parallel), and capping advisor tokens is the primary lever: setting reference_max_tokens (example 600) 'measurably cuts per-turn wall time with little quality impact'; reference outputs are appended as 'tail tokens' so prompt caching is preserved.
    EVIDENCE: "turn wall time correlates strongly with how many tokens the advisors emit"; reference_max_tokens=600 "measurably cuts per-turn wall time with little quality impact"; "reference outputs append as 'tail tokens' without breaking cache."
    SRC: https://hermes-agent.nousresearch.com/docs/user-guide/features/mixture-of-agents
[68] (moa) MoA presets are selectable per-surface and per-run: a `/moa` command runs a single prompt through the default preset then restores the user's prior model, and 'Presets are selectable on every Hermes surface.'
    EVIDENCE: "Presets are selectable on every Hermes surface"; "/moa runs a single prompt through the default MoA preset, then restores whatever model you were on."
    SRC: https://hermes-agent.nousresearch.com/docs/user-guide/features/mixture-of-agents
[69] (moa) Self-MoA — aggregating multiple samples from a SINGLE top-performing model — beats standard mixed-MoA (proposers = several different LLMs) by 6.6 points on AlpacaEval 2.0 (65.7% vs 59.1% LC win rate) and by an average of 3.8% across MMLU, CRUX, and MATH, with the same aggregator.
    EVIDENCE: Paper reports Self-MoA '6.6% improvement over MoA on AlpacaEval 2.0' and '3.8% improvement across MMLU, CRUX, and MATH'; 65.7 vs 59.1 win rate.
    SRC: https://arxiv.org/abs/2502.00674
[70] (moa) The core finding is that mixing different LLMs 'often lowers the average quality of the models', and MoA output quality is far more sensitive to proposer QUALITY than to DIVERSITY: regression coefficients make quality's effect ~1.4x-3.2x stronger than diversity (MATH quality alpha=4.719 vs diversity beta=2.839; CRUX 4.548 vs 1.421; MMLU 2.558 vs 1.841; all p<0.001, R2~=0.7-0.77).
    EVIDENCE: Table 4 regression: quality coefficient consistently exceeds diversity 1.4x-3.2x; 'MoA performance is rather sensitive to the quality.'
    SRC: https://arxiv.org/abs/2502.00674
[71] (moa) Heterogeneous mixing helped only marginally and only under a narrow condition: when proposers had COMPARABLE baseline quality (mixed configs beat Self-MoA by just 0.17-0.35% in Table 6). The optimal operating region is 'high quality and relatively low diversity' (bottom-right of quality-diversity plots), NOT maximum diversity.
    EVIDENCE: Table 6 mixed configs 59.86-60.04% vs Self-MoA 59.69% (0.17-0.35% margin); Fig 2 optimum = high-quality/low-diversity region.
    SRC: https://arxiv.org/abs/2502.00674
[72] (moa) Where heterogeneous mixing DOES yield real gains is task-specialized proposers on their own domain (e.g., Qwen2-Math-7B on MATH, DeepSeek-Coder-V2-Lite on CRUX) — i.e., complementary domain expertise, not generic model diversity. A domain-best proposer paired with a domain aggregator produced the largest lifts.
    EVIDENCE: Specialized-task setup uses domain models per benchmark; 'DeepSeek on CRUX achieved 50.75% with TaskBest aggregator' — gains from domain expertise.
    SRC: https://arxiv.org/abs/2502.00674
[73] (moa) Self-MoA-Seq aggregates an arbitrary number of outputs via a sliding window (window size 6, with the first 3 slots reserved for the previous round's synthesized output), matching the quality of aggregating all outputs at once while staying inside context-length limits — enabling incremental/streaming aggregation over many rounds.
    EVIDENCE: Self-MoA-Seq: sliding window size 6, first 3 slots hold prior-round synthesis; 'as effective as aggregating all outputs at once.'
    SRC: https://arxiv.org/abs/2502.00674
[74] (moa) The standard experimental architecture is a 2-layer MoA with 6 proposer samples and a single aggregator, and the aggregator can be the SAME model as one of the proposers (e.g., Qwen1.5-110B-Chat aggregates its own and others' samples). Self-MoA simply sets all 6 proposer samples to the single best model at temperature 0.7.
    EVIDENCE: 6 proposers, 2-layer architecture, aggregator Qwen1.5-110B-Chat (also a proposer); Self-MoA = 6 samples from one best model.
    SRC: https://arxiv.org/abs/2502.00674
[75] (provider-abstraction) LiteLLM is a Python SDK that calls 100+ LLM providers (OpenAI, Anthropic, Google Gemini/Vertex, Azure, AWS Bedrock, Cohere, Mistral, Groq, etc.) through a single OpenAI-format completion() function; latest release on the repo is v1.92.0 dated July 12, 2026.
    EVIDENCE: 'LiteLLM supports 100+ LLM providers'; `from litellm import completion; response = completion(model="openai/gpt-4o", messages=[{"role":"user","content":"Hello!"}])`; page shows v1.92.0 as latest release dated July 12, 2026.
    SRC: https://github.com/BerriAI/litellm
[76] (provider-abstraction) Model choice is per-call via a `model="provider/model"` string (e.g. openai/gpt-4o, anthropic/claude-...), and a Router adds retry/fallback logic plus application-level load balancing across multiple deployments — so different calls can be routed to different providers/models.
    EVIDENCE: completion(model="openai/gpt-4o", ...); README references a 'Router' with 'retry/fallback logic across multiple deployments' and 'application-level load balancing.'
    SRC: https://github.com/BerriAI/litellm
[77] (provider-abstraction) LiteLLM normalizes reasoning across providers: it accepts a unified reasoning_effort param (e.g. reasoning_effort="low") AND a passthrough thinking={"type":"enabled","budget_tokens":1024} for Anthropic; responses expose reasoning_content 'across all providers' and thinking_blocks for Anthropic. Reasoning is supported for Deepseek, Anthropic, Bedrock, OpenAI Responses API, Vertex AI, OpenRouter, XAI, Google AI Studio, Perplexity, Mistral AI, Groq.
    EVIDENCE: docs/reasoning_content shows reasoning_effort="low" and thinking={"type":"enabled","budget_tokens":1024}; 'reasoning_content ... Returned across all providers'; 'thinking_blocks ... Only returned for Anthropic models'; provider list as quoted. (Note: no explicit reasoning_effort->budget_tokens mapping table was shown on the page.)
    SRC: https://github.com/BerriAI/litellm
[78] (subscription-auth) LiteLLM authenticates per-provider using API keys read from environment variables (os.environ["OPENAI_API_KEY"], os.environ["ANTHROPIC_API_KEY"]); the proxy layer adds 'virtual keys.' There is no built-in OAuth or subscription-token (ChatGPT/Claude subscription) authentication — so LiteLLM alone does NOT deliver OpenClaw-style subscription auth.
    EVIDENCE: SDK 'uses environment variables per provider (os.environ["OPENAI_API_KEY"], os.environ["ANTHROPIC_API_KEY"])'; proxy 'supports virtual keys for secure access control'; no mention of OAuth/subscription tokens anywhere on the repo/gateway pages.
    SRC: https://github.com/BerriAI/litellm
[79] (provider-abstraction) LiteLLM supports synchronous and asynchronous (acompletion) calls plus streaming, and returns a consistent OpenAI ChatCompletion-shaped object regardless of provider (accessed via response.choices[0].message...).
    EVIDENCE: README notes 'support for both synchronous and asynchronous operations'; docs access fields via response.choices[0].message.reasoning_content / .thinking_blocks, i.e. the OpenAI ChatCompletion response schema across providers.
    SRC: https://github.com/BerriAI/litellm
[80] (provider-abstraction) The LiteLLM Proxy / AI Gateway is an OPTIONAL layer (the SDK works standalone) that adds centralized auth, multi-tenant cost tracking and spend management per project/user, guardrails, caching, an admin dashboard UI, and observability callbacks (Langfuse, MLflow, Lunary).
    EVIDENCE: AI Gateway offers 'centralized API authentication, multi-tenant cost tracking, per-project customization (logging, guardrails, caching), admin dashboard UI'; 'observability callbacks (Lunary, MLflow, Langfuse, etc.)'; the completion() SDK examples run without any proxy.
    SRC: https://github.com/BerriAI/litellm

=== REFUTED_WITH_CORRECTIONS (0) ===

=== PROVISIONAL_UNVERIFIED (0) ===

=== DESIGN IMPLICATIONS (42) ===
- To mimic OpenClaw's 'use your Claude subscription instead of an API key' path, TineyIC can shell out to `claude setup-token` (if Claude Code is installed) to mint a one-year subscription OAuth token and inject it as CLAUDE_CODE_OAUTH_TOKEN / Authorization: Bearer on the Anthropic endpoint — no API key stored — but must surface the constraints: token is inference-only and requires a Pro/Max/Team/Enterprise plan, and reusing subscription auth in a third-party app is an unofficial pattern this doc does not sanction (ToS caution).  [https://code.claude.com/docs/en/authentication]
- The onboarding flow should copy Claude Code's dual-path, precedence-aware model: offer 'log in with subscription (browser OAuth)' vs 'paste an API key', and critically implement the paste-a-code fallback for headless notebook contexts (JupyterHub/SSH/containers) where a localhost OAuth callback can't be reached — plus reuse existing local creds (macOS Keychain, ~/.claude/.credentials.json).  [https://code.claude.com/docs/en/authentication]
- For the model-agnostic layer, mirror the documented env-var contract as TineyIC's per-persona credential resolution: distinguish Bearer-token gateways (ANTHROPIC_AUTH_TOKEN) from direct API keys (ANTHROPIC_API_KEY as X-Api-Key), allow ANTHROPIC_BASE_URL overrides for proxies, and adopt a short refresh/expiry-warning loop so long unattended debates don't stall when a subscription OAuth login lapses.  [https://code.claude.com/docs/en/authentication]
- Onboarding template (onboarding-ux): copy OpenClaw's inference-first, verify-and-repair flow for our onboarding cell/page -- auto-detect env API keys AND locally-authenticated AI CLIs (Codex CLI, Claude Code), gate progress on a real 1-token completion, persist only the verified route, and treat re-runs as non-destructive verification passes that never overwrite a working config.  [https://docs.openclaw.ai/start/wizard]
- Provider abstraction + per-persona routing (provider-abstraction/moa): model our model-agnostic layer on OpenClaw's small wire-format taxonomy (OpenAI Chat / OpenAI Responses / Anthropic / auto-detect -- maps cleanly onto LiteLLM/any-llm) and adopt its per-agent 'auth profile + --model' primitive so each debate persona can target a different provider and a separate synthesizer model writes the memo (the MoA proposers/aggregator mapping).  [https://docs.openclaw.ai/start/wizard]
- Subscription auth + token storage (subscription-auth): offer BOTH a distinct OAuth/subscription login path (as OpenClaw separates 'ChatGPT/Codex setup' with its gpt-5.6-sol default from 'API-key setup') and a masked API-key fallback; prefer env-backed/exec secret references over plaintext for tokens. Research note: this page has zero content on reasoning/thinking-effort mapping and nothing on mixture-of-agents, and its 'Hermes' is only a legacy migration source (--flow import, /install/migrating-hermes), NOT Nous Research's Hermes MoA -- keep those separate when sourcing MoA design.  [https://docs.openclaw.ai/start/wizard]
- Model TineyIC's onboarding fork directly on Codex: present two first-class paths — 'Sign in with ChatGPT' (browser OAuth handoff, plus a device-code fallback for headless/remote notebook kernels) and 'Paste an API key' — then normalize both into one credential object the debate engine consumes, since Codex proves subscription-vs-key can be interchangeable at the transport layer.  [https://learn.chatgpt.com/docs/auth]
- Do not reinvent OpenAI credential handling for the subscription path: read Codex's existing `~/.codex/auth.json` (honoring `cli_auth_credentials_store`/OS keyring) so a user already logged into Codex CLI needs no re-auth, and lean on Codex's automatic access/refresh-token rotation instead of implementing our own OAuth refresh loop — this is likely exactly the mechanism OpenClaw reuses to ride a ChatGPT subscription.  [https://learn.chatgpt.com/docs/auth]
- Treat plan eligibility and ToS as an open risk to verify elsewhere: this auth doc never states which consumer tiers (Plus/Pro) may drive Codex programmatically, and non-interactive use is explicitly admin-gated on Enterprise — so before the UI promises 'use your ChatGPT subscription,' confirm per-plan entitlement and the terms posture rather than inferring it from the login flow working.  [https://learn.chatgpt.com/docs/auth]
- Direct blocker for the brief's requirement (2): the OpenClaw-style pattern of having TineyIC route a user's Claude Pro/Max subscription via OAuth is explicitly disallowed by Anthropic for third-party products. For the Anthropic provider, TineyIC must NOT ship a 'Log in with Claude.ai' integration as a product feature; default to API-key auth (Claude Console) or Bedrock/Vertex instead.  [https://code.claude.com/docs/en/legal-and-compliance]
- Onboarding (requirement 3) should present the API key as the primary compliant Anthropic path (with Bedrock/Vertex as enterprise options). Any 'reuse your existing subscription' convenience is only defensible if the user personally runs the official Claude Code / CLI locally on their own machine — TineyIC's own code must not capture, store, or proxy the subscription OAuth token on the user's behalf.  [https://code.claude.com/docs/en/legal-and-compliance]
- Because enforcement can occur without notice and Pro/Max limits assume 'ordinary, individual usage,' a 6-persona debate that fans out many concurrent calls on one subscription risks throttling or bans; design the per-persona model router to assume metered API keys as the baseline for Anthropic, and surface this ToS caveat explicitly in onboarding copy and docs.  [https://code.claude.com/docs/en/legal-and-compliance]
- Subscription auth: adopt OpenClaw's split — a per-persona/per-debate encrypted auth-profile store holding {access,refresh,expires,accountId} plus API keys, with OAuth+PKCE for OpenAI/Codex (loopback callback on a fixed localhost port + a paste-the-redirect fallback, essential for headless notebook kernels) and REUSE of the user's existing Claude Code CLI credentials for Anthropic rather than minting our own Claude OAuth. Gate Anthropic-subscription mode behind an explicit ToS warning and default to API keys: OpenAI/Codex subscription use is officially sanctioned, but Anthropic's is legally contested (reportedly blocked Apr 2026). Critically, warn that TineyIC refreshing a subscription token can log the user out of their own Claude Code/Codex CLI (the 'token sink' mutual-invalidation hazard) — prefer read-through reuse of the CLI's tokens over taking ownership, and never wrap OAuth material in a static secret ref.  [https://github.com/openclaw/openclaw]
- Provider abstraction + thinking dial: key everything on `provider/model` and give each provider a thin adapter owning auth, catalog, failover, and a `resolve_thinking_profile()`. In Python, LiteLLM (or any-llm) covers API-key inference/routing for our target providers out of the box, but — as OpenClaw demonstrates by shipping LiteLLM as just ONE of ~100 plugins — it will NOT cover Codex/Claude SUBSCRIPTION OAuth, so layer a small OAuth/credential module on top of it. Replicate OpenClaw's exact normalized effort ladder (off…minimal…low…medium…high…xhigh…adaptive…max) and its per-provider mapping table for our per-persona 'thinking effort' dial (OpenAI reasoning_effort, Anthropic thinking budgets/effort, Gemini thinkingBudget), rejecting or remapping levels a given model can't support.  [https://github.com/openclaw/openclaw]
- Committee/MoA + onboarding: OpenClaw validates our 'each persona on a different model/account' design via per-agent isolation of model registry + auth profiles — mirror that with a per-persona model+credential binding — but since it has no real MoA aggregator, plan our own synthesizer model step to write the memo rather than expecting the framework to provide it. For onboarding, copy the `openclaw onboard --auth-choice {openai(OAuth)|apiKey|openai-api-key}` pattern: a step-by-step flow whose first branch is 'connect a subscription (OAuth) OR paste an API key,' backed by doctor-style health checks (`models status --probe` reason codes such as missing_credential/expired/excluded_by_auth_order) surfaced directly in the notebook onboarding cell.  [https://github.com/openclaw/openclaw]
- Adopt OpenClaw's layered config model for TineyIC: make each persona's (a) model ref via a `provider/model` id, (b) credential mode (subscription OAuth vs API key), and (c) normalized thinking-effort three INDEPENDENT knobs. This directly enables the per-persona-different-model requirement, and add an ordered per-provider failover list (subscription-first, API-key backup) that rotates on usage-limit blocks until the provider's reset time so a phase-by-phase debate never hard-fails mid-cell.  [https://docs.openclaw.ai/providers/openai]
- Mirror the credential mechanics: store subscription OAuth tokens in TineyIC's own isolated credential store and hand them to the model transport directly rather than leaking API keys as env vars into the Jupyter kernel; support BOTH a localhost-callback OAuth flow and a device-code flow (device-code is essential for remote/headless kernels where a localhost callback can't reach the user's browser); optionally bridge an existing Codex CLI / Claude Code login as a fallback token source; and restrict to official HTTPS endpoints only. Implement `thinking_effort` as a normalized enum GATED per-model by a capability table (OpenAI reasoning_effort tiers incl. none/xhigh/max, Anthropic budget tokens, Gemini thinking budget), silently omitting the field for models that reject it -- exactly OpenClaw's 'keep none only where supported' pattern.  [https://docs.openclaw.ai/providers/openai]
- Model the onboarding page as OpenClaw's two-branch chooser -- 'Connect subscription' vs 'Enter API key' -- each with a 'best for' explainer and a concrete verify/echo step (confirm the model is reachable) before the first debate cell runs, plus a live per-persona runtime/auth indicator in the town-hall widget (analogous to `/status` -> 'Runtime: OpenAI Codex'). CAUTION: OpenClaw asserts 'OpenAI explicitly supports subscription OAuth' with zero citation; independently verify the Codex/Claude subscription-OAuth ToS before shipping subscription auth as a default and surface an explicit risk disclaimer in that onboarding branch.  [https://docs.openclaw.ai/providers/openai]
- Model-agnostic per-persona selection: adopt OpenClaw's canonical 'provider/model' ref scheme plus a per-debate/per-persona allowlist (its 'agents.defaults.models'), and crucially keep the BACKEND RUNTIME (direct API vs subscription-CLI reuse) as a SEPARATE axis from the model id (its per-model 'agentRuntime.id'). That lets one persona's 'anthropic/claude-opus' run on either an API key or the user's Claude subscription without changing the persona's declared model — a clean fit for our 'model per persona' requirement.  [https://github.com/openclaw/openclaw/blob/main/docs/concepts/model-providers.md]
- Subscription auth: mimic OpenClaw by NOT reverse-engineering provider OAuth ourselves — reuse the official CLIs (Codex app-server for ChatGPT, 'claude -p' for Anthropic Claude Code), and let those CLIs hold the OAuth tokens in their own on-host auth profiles rather than storing secrets in our notebook/config. OpenClaw frames exactly these CLI-reuse paths as 'sanctioned' and reserves its 'unofficial integration / account-restriction risk' ToS warning for reverse-engineered flows (Gemini CLI), so CLI reuse is the lower-ToS-risk pattern to copy; some flagship models (gpt-5.6-sol/codex-spark) are reachable ONLY via the subscription route, which is a selling point for supporting it.  [https://github.com/openclaw/openclaw/blob/main/docs/concepts/model-providers.md]
- Onboarding + normalized thinking-effort: build our onboarding page as a per-provider 'auth-choice' fork (connect a subscription OR paste an API key), mirroring 'openclaw onboard --auth-choice'; and implement our normalized 'thinking effort' knob the OpenClaw way — one session-level level that each provider adapter translates into its native param (OpenAI reasoning_effort, Anthropic thinking budget, Gemini thinkingBudget/thinkingLevel, vLLM enable_thinking), rather than exposing raw per-provider fields. Note: this page contains nothing on mixture-of-agents, so MoA design must be sourced elsewhere.  [https://github.com/openclaw/openclaw/blob/main/docs/concepts/model-providers.md]
- Mirror OpenAI's own dual-path onboarding verbatim: present 'Sign in with ChatGPT (subscription, usage included)' and 'Enter an API key (per-token)' as two first-class connect options, with a link-out to the provider's auth doc (/docs/auth). This page proves subscription auth is an officially-supported Codex CLI mode -- the exact mechanism OpenClaw piggybacks on -- but note it says nothing about third-party token reuse, so the ToS standing of using these subscription tokens inside TineyIC must be verified from the auth/policy pages, not inferred from this pricing page.  [https://learn.chatgpt.com/docs/pricing]
- Design the debate engine around subscription rate-limit reality: subscription auth is a rolling 5h message budget (Plus is only ~20-110 balanced-model messages/5h, and that pool is SHARED across all agentic features), so a 6-persona multi-phase 'town hall' can exhaust it fast. Build a live budget/usage meter (equivalent to Codex '/status'), let users assign cheaper model tiers (Luna-style) to high-volume personas, and support automatic API-key overflow mid-debate -- a hybrid fallback OpenAI itself endorses ('run extra local tasks using an API key').  [https://learn.chatgpt.com/docs/pricing]
- Map the normalized 'thinking effort' control per auth mode: on API-key auth use the real reasoning_effort parameter; on subscription auth, effort is instead expressed as model-tier choice (quality/balanced/fast, mirroring Sol/Terra/Luna) plus a Fast-mode speed flag -- there is no numeric reasoning budget on the subscription surface. Per-persona model selection in TineyIC maps cleanly onto this in-session model-switching the CLI already exposes.  [https://learn.chatgpt.com/docs/pricing]
- The debate committee maps cleanly onto MoA: the 6 value-investor personas = proposers (each freely assignable to a DIFFERENT model per the model-agnostic requirement, since MoA validates heterogeneous multi-model ensembles), and a dedicated synthesizer model that writes the memo = the aggregator. MoA is direct empirical backing for the per-persona-model + separate-synthesizer architecture.  [https://arxiv.org/abs/2406.04692]
- Route models by role, not just by 'best overall': treat proposer-capability and aggregator/synthesizer-capability as separate axes. Reserve a versatile, strong model (GPT-4o/Qwen/LLaMA-3-class, or a high-thinking-effort reasoning model) for the memo-writing synthesizer, and allow specialist or cheaper models to serve as persona proposers — a strong persona model is not guaranteed to be a good synthesizer. This should shape the model-picker defaults in onboarding and the persona/synthesizer config.  [https://arxiv.org/abs/2406.04692]
- Keep the debate shallow to control notebook cost/latency: MoA-Lite shows a 2-layer (roughly 2-round) committee with a single aggregator already beats a frontier single model, and the 'collaborativeness' result confirms personas benefit from seeing each other's prior turns — which validates the phase-by-phase persistent 'town hall' where each persona reads earlier speeches before responding, without needing many layers.  [https://arxiv.org/abs/2406.04692]
- The proposers->aggregator pattern maps almost 1:1 onto TineyIC's committee: run each of the 6 personas as a 'proposer' turn per phase, then have a dedicated synthesizer model write the memo by injecting the numbered concatenation of persona speeches into an aggregator prompt (reuse inject_references_to_messages verbatim). MoA 'layers'/--rounds ≈ our debate rounds; the synthesis prompt's 'may be biased or incorrect' framing is a ready template for a skeptical memo-writer.  [https://github.com/togethercomputer/MoA]
- MoA empirically validates 'each persona on a different model' (heterogeneous proposers beat one frontier model, 65.1% vs 57.5%), but the reference impl still funnels every agent through ONE Together client — exactly the single-global-client limitation TineyIC has today. To meet the per-persona multi-provider requirement we must add the routing layer MoA lacks (LiteLLM/any-llm or per-agent OpenAI-compatible clients), and add per-turn streaming + thinking capture (MoA streams only the aggregator).  [https://github.com/togethercomputer/MoA]
- Apache-2.0 lets us vendor MoA's aggregator prompt and reference-injection logic directly; but MoA provides no normalized thinking-effort control and no subscription/OAuth auth (only TOGETHER_API_KEY / OPENAI_API_KEY env vars), so the model-agnostic thinking-effort mapping and OpenClaw-style subscription auth are entirely net-new work not informed by this source.  [https://github.com/togethercomputer/MoA]
- Subscription auth: copy Hermes' concrete mechanism, which is a documented open-source implementation of the OpenClaw pattern -- device-code OAuth that reuses the official CLI credential stores (`~/.codex/auth.json`, Claude Code's cred store) and re-persists into our own `auth.json`, and surface the OAuth(=subscription billing) vs API-key(=standard API pricing) distinction explicitly in onboarding so users understand what their ChatGPT/Claude Max login is being charged against.  [https://github.com/NousResearch/hermes-agent]
- MoA/committee mapping: adopt Hermes' `auxiliary.*.provider` per-task routing shape for our debate -- one config block maps each role (every persona/proposer and the synthesizer/memo writer) to its own `provider:model`, defaulting to `auto` (inherit the debate's main model) so users can start with one model and progressively assign different providers per persona; Hermes shows this cross-provider-per-subtask design is practical in a uv/Python CLI.  [https://github.com/NousResearch/hermes-agent]
- Thinking effort: implement a single normalized `reasoning_effort` ladder (xhigh/high/medium/low/minimal/none) surfaced as the per-persona 'thinking effort' knob, with per-provider adapters (OpenAI reasoning_effort, Anthropic thinking budget, Gemini thinking budget, plus a 'reasons by default / no param' case like Grok and an `extra_body` passthrough for self-hosted) -- mirroring exactly how Hermes maps one label set onto heterogeneous provider params.  [https://github.com/NousResearch/hermes-agent]
- MoA maps almost 1:1 onto our committee: model each of the 6 personas as a 'reference model' with its own provider/model pair, and make the memo-writing synthesizer the 'aggregator' — but note Hermes deliberately strips tool schemas from proposers and lets ONLY the aggregator act, whereas our personas debate visibly; we want proposers to be full speakers and the synthesizer to be a non-acting aggregator that reads all turns as context.  [https://hermes-agent.nousresearch.com/docs/user-guide/features/mixture-of-agents]
- Adopt Hermes' 'preset' abstraction directly: named committee configs (per-persona provider/model + synthesizer model + per-role token caps/temperature) with a `default_preset`, selectable per-notebook-cell/per-debate — this is a proven schema for per-persona multi-provider routing and validates storing explicit provider/model pairs rather than a single global client.  [https://hermes-agent.nousresearch.com/docs/user-guide/features/mixture-of-agents]
- Two gaps to own ourselves: (a) Hermes exposes only per-model temperature and a `reference_max_tokens` latency knob — it has NO normalized cross-provider 'thinking effort' control, so our reasoning_effort/thinking-budget normalization is a genuine differentiator; (b) with 6 streaming personas, borrow the token-cap lever (per-persona max_tokens) since wall time scales with total proposer tokens, and keep synthesizer input as appended 'tail' context to preserve prompt caching.  [https://hermes-agent.nousresearch.com/docs/user-guide/features/mixture-of-agents]
- Directly challenges the brief's premise that each persona should run on a DIFFERENT model for better output. MoA evidence shows heterogeneous mixing usually drags final quality DOWN (quality dominates diversity ~1.4-3.2x). For TineyIC, persona/viewpoint diversity should come from PROMPTS/personas, while the memo SYNTHESIZER (aggregator) should be a single top-tier model; per-persona model choice is best framed as a user cost/preference knob, not a quality-boosting strategy.  [https://arxiv.org/abs/2502.00674]
- If personas DO run on different models, keep them within one comparable quality tier and assign each model to a domain where it excels (the paper's only reliable win condition: task-specialized proposers). E.g., route the valuation/quant persona to a strong-reasoning model and a coding-heavy check to a code model — but never mix a clearly weaker model in merely to 'add a voice', since that measurably lowers the synthesized memo.  [https://arxiv.org/abs/2502.00674]
- For the synthesizer aggregating 6 personas across multiple debate phases, adopt a Self-MoA-Seq-style sliding-window aggregation (fixed window, reserve slots for the running synthesis) rather than concatenating every turn into one giant aggregator prompt — it bounds context growth, supports streaming/phase-by-phase memo building, and matches full-batch quality; note the aggregator may be one of the persona models.  [https://arxiv.org/abs/2502.00674]
- Adopt LiteLLM as the model-agnostic core: replace the vendored TinyTroupe single global OpenAI client with litellm.completion(model='provider/model'), giving per-debate AND per-persona model selection (each of the 6 personas can run a different provider/model) behind one call signature, with the Router providing fallbacks — this directly satisfies Requirement 1 and is a natural fit for a Python notebook product on py3.12/uv.  [https://github.com/BerriAI/litellm]
- Implement the normalized 'thinking effort' control on top of LiteLLM's unified reasoning_effort (low/medium/high) plus thinking={'type':'enabled','budget_tokens':N} passthrough; expose low/med/high in the widget and let LiteLLM translate per provider, then read response.choices[0].message.reasoning_content / thinking_blocks to feed the expandable per-turn 'thinking' UI. Verify/curate the reasoning_effort->budget_tokens mapping yourself since the docs page did not show an explicit table.  [https://github.com/BerriAI/litellm]
- LiteLLM does NOT solve OpenClaw-style subscription auth (it is API-key/env-var based, no ChatGPT/Claude OAuth), so plan a separate auth adapter that injects subscription OAuth tokens; and treat the LiteLLM Proxy/AI Gateway as optional heavyweight infra — the SDK alone (with a local JSONL event log for cost/observability) is sufficient for a notebook-first, run-locally product.  [https://github.com/BerriAI/litellm]